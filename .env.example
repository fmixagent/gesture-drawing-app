# Copy this file to .env and adjust values for your deployment.
# .env is gitignored so each deployment (electron-vite desktop build vs vite-react web/PWA build) can set its own values.

# true  -> building/running the vite-react (web/PWA) version
# false -> building/running the electron-vite (desktop) version
WEB_VERSION=true
